# CI Audit — Run 27384882938 (commit ee24fd7)

**Date:** 2026-06-12  
**Workflow:** build-launcher.yml  
**Result:** FAILURE (all jobs)

## Jobs

| Job | Status | Conclusion |
|-----|--------|------------|
| build-windows | completed | **failure** |
| build-macos | completed | **failure** |
| deploy-vps | completed | **skipped** |

## build-windows — blocking errors

1. **E0603** — `constant import CDN_BASE is private`
   - File: `launcher/src-tauri/src/updater.rs`
   - Cause: imported `CDN_BASE` from `api` module (not re-exported)
   - Fix: `use crate::paths::CDN_BASE` (commit f853e00)

2. **Send trait** — `future returned by bootstrap is not Send`
   - File: `launcher/src-tauri/src/lib.rs:210`
   - Cause: `std::sync::Mutex` in AppState + non-Send progress callback
   - Fix: `AtomicBool` + `Fn(u64,u64) + Send + Sync` (commit f853e00)

## build-macos — blocking errors

Same Rust compile errors as Windows (E0603 + Send).

## deploy-vps

Skipped because `build-windows` failed (`needs: [build-windows]`).

## Non-blocking warnings

- Node.js 20 deprecation on GitHub Actions runners (checkout, setup-node)
- macOS job uses `continue-on-error: true` on build step (by design)

## Deploy blockers (independent of Rust)

1. `scripts/darkworld.nsi` installs **game** (`DarkWorld.exe`), not launcher
2. `deploy-launcher-artifacts.sh` calls wrong NSIS script
3. `DEPLOY_HOST` / `DEPLOY_SSH_KEY` secrets may be unset — VPS uses `fetch-github-launcher.sh`

## CDN state at audit time

- `DarkWorld-Launcher.exe`: 62,664,008 bytes, hash `82791f8c653afbfbb4c8e4a3622ad0e36b61c98c71b15403d3f9572852525d64`
- Type: Node.js CLI (pkg), not Tauri GUI

// Death + Afterlife — Test Suite
describe("DeathAfterlife", () => {
  it("should send character to afterlife on death", async () => {
    expect(true).toBe(true);
  });
  it("should return character from afterlife", async () => {
    expect(true).toBe(true);
  });
});
